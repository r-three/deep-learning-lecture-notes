---
title: "Lecture 11: Architecture Grab Bag, Part 2 — Transposed Convolution, the U-Net, Autoencoders, and VAEs"
description: "More architectural ideas: autoencoders and the variational autoencoder for unsupervised representation learning, the transposed convolution as the backward pass of a convolution, and the U-Net for dense image-to-image prediction."
bibliography: references.bib
---

```{=html}
<style>#title-block-header { display: none; }</style>
<div class="lecture-header">
  <h1>Lecture 11: Architecture Grab Bag, Part 2 — Transposed Convolution, the U-Net, Autoencoders, and VAEs</h1>
</div>
```

**Overview.** We continue pulling things out of the architecture grab bag. The bulk of this lecture is the **variational autoencoder** (VAE), a deep generative model built on top of the **autoencoder**, which we motivate through unsupervised representation learning. We will build the objective piece by piece — a reconstruction term and a regularization term — then show that the same objective falls out of a probabilistic model when we apply variational inference, and finally work through the practical choices that make it trainable, including the reparameterization trick. After that come two smaller topics that fit naturally with autoencoders of images: the **transposed convolution**, a convolution-like operation that makes feature maps *larger*, and the **U-Net**, an encoder-decoder architecture for producing an output image the same size as the input. The topics are largely independent.

## Representation Learning {#sec-representation}

It helps to motivate the variational autoencoder through representation learning, which has in some sense already been a focus of this course. In old-fashioned machine learning, we design a set of features by hand, collect them on our data, and train a simple classifier on those feature values. In deep learning, we instead feed raw data into a neural network and ask the network to *learn* features that are useful for the task. When we say we are learning features, we can equally say we are learning **representations** of the data.

So far, that representation learning has been an accidental side effect of supervised training: we build a network, train it with labels, and useful representations emerge along the way. But we can also pursue representation learning more broadly, and in particular **unsupervised** representation learning, where finding a representation useful for downstream tasks is the *explicit* goal of a stage that uses no labels at all.

With unlabeled data it is not obvious what the objective should be. One way to write one down is this: we want a representation that contains *as little information as possible* about the input, while still allowing us to reconstruct the input. Put differently, we want to compress the salient information about the input into a small representation, so that there is as little information in it as possible, yet still recover the original input in a perceptually accurate way. This pairing — **compression** and **reconstruction** — is the framing we will formalize.

An example makes the idea concrete. Suppose our data is $32 \times 32$ binary images of handwritten digits, each pixel either black ($0$) or white ($1$). How much information does one image contain? We can measure information by storage: there are $32 \times 32 = 1024$ bits in each image. The question representation learning asks is whether we can find a representation that takes far fewer than $1024$ bits to store, yet still reconstructs the image accurately.

Consider what such a representation might encode. Which digit is it — a $0$ through $9$? That takes about four bits. How thick are the strokes? Perhaps a handful of thickness levels. How slanted is it, left or right? For a $1$ specifically, does it have the little flag at the top, or the base at the bottom? Each of these is a salient factor, and if we encode the answers to enough of them, we can produce a reconstruction that looks much like the original — most importantly of the same digit, since an image of a zero is plainly different from an image of a one. Written down and encoded, these factors likely take fewer than $1024$ bits.

The payoff is that a representation which compresses these salient factors is often *more useful* for downstream tasks than the raw data. If the representation already encodes the digit's identity — because identity is the most salient factor for reconstruction — then a small classifier reading that representation can classify the digit easily, whereas reading the digit off the raw pixels is exactly the kind of thing that is hard to do by hand (Lecture 1). This can feel like a chicken-and-egg problem: how can a representation encode digit identity without being trained on labels? But clustering is a familiar example of uncovering class structure without supervision, so there is reason to hope that a good compression-and-reconstruction objective can surface salient factors on its own. Informally, then, the most salient factors for reconstruction are often the most useful for downstream tasks.

This is not the only route to unsupervised representations. Masked language modeling, which produced the BERT embeddings used on an earlier homework (Lecture 8), looks nothing like compression-and-reconstruction and is also unsupervised. But compression-and-reconstruction is one effective way to formalize the problem, and — as we will see — it needs no labeled data.

## Autoencoders {#sec-autoencoder}

The architecture we use to attempt this is the **autoencoder**. It takes an input $\bm{x}$ — say, one of the binary handwritten images — and passes it through a sequence of layers that produce representations which get *smaller and smaller*, then through a further sequence of layers that make the representations *bigger and bigger*, until the final layer maps back to the dimensionality of the input. The output is a reconstruction of the input, which we write $\tilde{\bm{x}}$.

The shrinking half is the **encoder**; the growing half is the **decoder**. The smallest representation in the middle is the **bottleneck**, more commonly called the **latent code**, conventionally written $\bm{z} \in \mathbb{R}^d$.

```{=html}
<div style="overflow-x:auto; margin:1.5rem 0;">
<svg viewBox="0 0 680 300" width="100%" style="max-width:680px;display:block;margin:auto;font-family:Helvetica,'Helvetica Neue',Arial,sans-serif;"><defs><marker id="a" markerWidth="7" markerHeight="7" refX="6" refY="3.5" orient="auto"><path d="M0,0 L0,7 L7,3.5 z" fill="#888"/></marker></defs><text x="20" y="30" font-size="22" font-weight="600" fill="#333">Autoencoder</text><rect x="30" y="120" width="60" height="60" rx="4" fill="#C6DEF1" stroke="#5795C7" stroke-width="1.5"/><text x="60.0" y="156.0" text-anchor="middle" font-size="18" font-weight="600" fill="#205279">x</text><text x="38" y="205" font-size="13" fill="#666">input</text><rect x="130" y="104.0" width="16" height="92" rx="3" fill="#C9E4DE" stroke="#57C7AE" stroke-width="1.4"/><rect x="168" y="114.0" width="16" height="72" rx="3" fill="#C9E4DE" stroke="#57C7AE" stroke-width="1.4"/><rect x="206" y="124.0" width="16" height="52" rx="3" fill="#C9E4DE" stroke="#57C7AE" stroke-width="1.4"/><rect x="244" y="133.0" width="16" height="34" rx="3" fill="#C9E4DE" stroke="#57C7AE" stroke-width="1.4"/><line x1="90" y1="150" x2="128" y2="150" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><line x1="146" y1="150" x2="168" y2="150" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><line x1="184" y1="150" x2="206" y2="150" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><line x1="222" y1="150" x2="244" y2="150" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><line x1="260" y1="150" x2="300" y2="150" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><rect x="300" y="130" width="20" height="40" rx="3" fill="#DBCDF0" stroke="#8457C7" stroke-width="1.6"/><text x="310" y="200" text-anchor="middle" font-size="13" font-weight="600" fill="#442079">z</text><text x="310" y="218" text-anchor="middle" font-size="11" fill="#442079">latent code</text><text x="310" y="231" text-anchor="middle" font-size="11" fill="#442079">(bottleneck)</text><line x1="320" y1="150" x2="354" y2="150" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><rect x="356" y="133.0" width="16" height="34" rx="3" fill="#C9E4DE" stroke="#57C7AE" stroke-width="1.4"/><rect x="394" y="124.0" width="16" height="52" rx="3" fill="#C9E4DE" stroke="#57C7AE" stroke-width="1.4"/><rect x="432" y="114.0" width="16" height="72" rx="3" fill="#C9E4DE" stroke="#57C7AE" stroke-width="1.4"/><rect x="470" y="104.0" width="16" height="92" rx="3" fill="#C9E4DE" stroke="#57C7AE" stroke-width="1.4"/><line x1="372" y1="150" x2="394" y2="150" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><line x1="410" y1="150" x2="432" y2="150" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><line x1="448" y1="150" x2="470" y2="150" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><line x1="486" y1="150" x2="524" y2="150" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><rect x="524" y="120" width="60" height="60" rx="4" fill="#F7D9C4" stroke="#C78557" stroke-width="1.5"/><text x="554.0" y="156.0" text-anchor="middle" font-size="18" font-weight="600" fill="#794520">x&#771;</text><text x="510" y="205" font-size="13" fill="#666">reconstruction</text><text x="195" y="95" text-anchor="middle" font-size="14" font-weight="600" fill="#207965">encoder</text><path d="M132,105 Q195,88 258,105" fill="none" stroke="#207965" stroke-width="1.2"/><text x="425" y="95" text-anchor="middle" font-size="14" font-weight="600" fill="#207965">decoder</text><path d="M358,105 Q425,88 488,105" fill="none" stroke="#207965" stroke-width="1.2"/><text x="20" y="270" font-size="13" fill="#666">Layers shrink the representation to a latent code z, then grow it back; trained so x&#771; &#8776; x.</text></svg></div>
```

::: {.definition-block}
**Definition.** An *autoencoder* is a neural network composed of an *encoder* that maps an input $\bm{x}$ to a low-dimensional *latent code* $\bm{z}$ and a *decoder* that maps $\bm{z}$ back to a reconstruction $\tilde{\bm{x}}$ of the input. It is trained to make the reconstruction close to the input.

:::

We train the autoencoder with a **reconstruction loss**, for example the squared error between the input and its reconstruction:

$$\mathcal{L} = \lVert \bm{x} - \tilde{\bm{x}} \rVert^2.$$

This is an unsupervised loss: there are no labels involved. Given a data point, we feed it through the autoencoder, obtain an output, and drive down the reconstruction error. If we drive it to zero, we have preserved enough information through the bottleneck to reconstruct the original input.

Reconstruction was one of our two goals. The other was compression. Are we compressing? A natural thought is that if we make the latent code smaller than the input — $\bm{x}$ is $1024$-dimensional, say, and $\bm{z}$ only $16$-dimensional — then $\bm{z}$ holds fewer numbers, and so surely less information. But is that actually true?

It is not. Suppose the latent code is real-valued, $\bm{z} \in \mathbb{R}^d$. How much information can a vector of real values hold? In principle, an infinite amount: a single real number can take infinitely many distinct values — there are infinitely many reals between any two — and we have $d$ of them. (On a computer we of course use finite-precision floating point, so a `float64` latent stores at most $64 d$ bits. But even setting hardware aside, the conceptual problem stands.) So making $\dim(\bm{z}) < \dim(\bm{x})$ does *not* by itself guarantee compression.

::: {.callout-warning}
**Why not just make the latent discrete?** If real values are the problem, we might make $\bm{z}$ binary — say $16$ bits, holding at most $16$ bits of information. The trouble is that discreteness in the middle of the model blocks gradients: we cannot backpropagate through a hard discrete bottleneck (we will meet the same obstacle with the reparameterization trick below). So we tackle the information problem a different way.

:::

## Variational Autoencoders {#sec-vae}

The **variational autoencoder** keeps the reconstruction objective but adds a term that penalizes the amount of information in the latent code. Rather than constrain the latent to be small or discrete, we let it be a large, high-precision, real-valued vector — capable of storing plenty of information — and then *regularize* the model so it uses as little of that capacity as it can. You may set $\bm{z}$ to whatever you like for a given input, but the more information $\bm{z}$ carries about $\bm{x}$, the more you are penalized. The question is how to write that penalty down.

### The objective {#sec-vae-objective}

To measure and constrain information in the latent, we turn $\bm{z}$ into a random variable. The encoder no longer outputs a single latent code; it outputs the *parameters of a probability distribution* over latent codes, conditioned on the input. We write this distribution $q_\phi(\bm{z} \mid \bm{x})$ and call it the encoder. This is the same move the language model makes when it outputs the parameters of a categorical distribution to sample the next token from (Lecture 6): feed in $\bm{x}$, get a distribution, sample $\bm{z}$ from it.

The decoder is likewise probabilistic. We treat $\bm{x}$ as a random variable and let $p_\theta(\bm{x} \mid \bm{z})$ be a distribution over inputs given a latent code. So the full path is: take $\bm{x}$, feed it through the encoder $q_\phi$ to get a distribution over $\bm{z}$, sample a $\bm{z}$, feed that through the decoder $p_\theta$ to get a distribution over $\bm{x}$. For reconstruction, we want that final distribution to place high probability on the *original* $\bm{x}$.

The regularization term should measure how much information $\bm{z}$ carries about $\bm{x}$. The natural tool is the **KL divergence**, which measures the extra information needed to encode samples from one distribution if you have another. We take the KL divergence between the encoder $q_\phi(\bm{z} \mid \bm{x})$ and a **prior** $p(\bm{z})$ — a distribution over latents that is *unconditional* on $\bm{x}$, carrying no information about any particular input. If this KL divergence is small, then drawing from $q_\phi(\bm{z} \mid \bm{x})$ is nearly the same as drawing from the unconditional prior, so $q_\phi$ carries little information about $\bm{x}$. The only additional information in $q_\phi(\bm{z} \mid \bm{x})$ beyond the prior *is* $\bm{x}$, so this KL is a measure of the information used to encode $\bm{x}$.

Putting the two terms together — and noting that this is an *objective* we **maximize**, not a loss we minimize — the VAE objective is:

::: {.boxed-eq}
$$
\underbrace{\mathbb{E}_{\bm{z} \sim q_\phi(\bm{z} \mid \bm{x})} \big[\log p_\theta(\bm{x} \mid \bm{z})\big]}_{\text{reconstruction}}
\;-\;
\underbrace{\operatorname{KL}\!\big(q_\phi(\bm{z} \mid \bm{x}) \,\big\|\, p(\bm{z})\big)}_{\text{regularization}}.
$$
:::

```{=html}
<div style="overflow-x:auto; margin:1.5rem 0;">
<svg viewBox="0 0 720 320" width="100%" style="max-width:720px;display:block;margin:auto;font-family:Helvetica,'Helvetica Neue',Arial,sans-serif;"><defs><marker id="a" markerWidth="7" markerHeight="7" refX="6" refY="3.5" orient="auto"><path d="M0,0 L0,7 L7,3.5 z" fill="#888"/></marker></defs><text x="20" y="30" font-size="22" font-weight="600" fill="#333">Variational autoencoder and the reparameterization trick</text><rect x="30" y="120" width="58" height="60" rx="4" fill="#C6DEF1" stroke="#5795C7" stroke-width="1.5"/><text x="59.0" y="156.0" text-anchor="middle" font-size="18" font-weight="600" fill="#205279">x</text><line x1="88" y1="150" x2="120" y2="150" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><rect x="120" y="116" width="96" height="68" rx="6" fill="#C9E4DE" stroke="#57C7AE" stroke-width="1.5"/><text x="168.0" y="148.0" text-anchor="middle" font-size="15" font-weight="600" fill="#207965">q&#966;(z|x)</text><text x="168.0" y="165.0" text-anchor="middle" font-size="13" fill="#207965">encoder</text><line x1="216" y1="136" x2="258" y2="136" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><line x1="216" y1="164" x2="258" y2="164" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><text x="272" y="140" text-anchor="middle" font-size="15" font-weight="600" fill="#207965">&#956;</text><text x="272" y="168" text-anchor="middle" font-size="15" font-weight="600" fill="#207965">&#963;</text><rect x="300" y="110" width="150" height="80" rx="8" fill="#fff" stroke="#8457C7" stroke-width="1.6" stroke-dasharray="5,3"/><text x="375" y="128" text-anchor="middle" font-size="12" font-weight="600" fill="#442079">reparameterize</text><text x="375" y="154" text-anchor="middle" font-size="16" font-weight="600" fill="#442079">z = &#956; + &#963;&#8901;&#949;</text><text x="375" y="176" text-anchor="middle" font-size="12" fill="#442079">&#949; ~ N(0, I)</text><line x1="286" y1="140" x2="300" y2="134" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><line x1="286" y1="160" x2="300" y2="166" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><circle cx="375" cy="80" r="18" fill="#FAEDCB" stroke="#C7A857" stroke-width="1.5"/><text x="375" y="85" text-anchor="middle" font-size="14" font-weight="600" fill="#796020">&#949;</text><line x1="375" y1="98" x2="375" y2="108" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><text x="398" y="86" font-size="13" fill="#666">noise &#949; ~ N(0, I)</text><text x="398" y="101" font-size="13" fill="#666">(constant w.r.t. gradients)</text><line x1="450" y1="150" x2="486" y2="150" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><text x="468" y="142" text-anchor="middle" font-size="14" font-weight="600" fill="#442079">z</text><rect x="486" y="116" width="96" height="68" rx="6" fill="#F7D9C4" stroke="#C78557" stroke-width="1.5"/><text x="534.0" y="148.0" text-anchor="middle" font-size="15" font-weight="600" fill="#794520">p&#952;(x|z)</text><text x="534.0" y="165.0" text-anchor="middle" font-size="13" fill="#794520">decoder</text><line x1="582" y1="150" x2="614" y2="150" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><rect x="614" y="120" width="58" height="60" rx="4" fill="#C6DEF1" stroke="#5795C7" stroke-width="1.5"/><text x="643.0" y="156.0" text-anchor="middle" font-size="18" font-weight="600" fill="#205279">x&#771;</text><text x="20" y="290" font-size="13" fill="#666">The encoder outputs &#956; and &#963;; the latent z is a differentiable function of them plus fixed noise &#949;,</text><text x="20" y="308" font-size="13" fill="#666">so gradients flow to the encoder. The decoder maps z to a distribution over reconstructions.</text></svg></div>
```

The first term is a reconstruction term in disguise. When the decoder $p_\theta(\bm{x} \mid \bm{z})$ — also called the **likelihood model** — is an identity-covariance Gaussian, its log density is, up to constants, the negative squared error between $\bm{x}$ and the Gaussian's mean. The density of a Gaussian carries a factor $e^{-\lVert \bm{x} - \bm{\mu} \rVert^2 / (2\sigma^2)}$; with unit variance the exponent is a negative squared error, and taking the log leaves exactly that. So maximizing $\mathbb{E}[\log p_\theta(\bm{x} \mid \bm{z})]$ under a Gaussian likelihood *is* minimizing mean squared error between the input and the decoder's mean. The second term, as discussed, loosely measures the information in $\bm{z}$ about $\bm{x}$.

The two terms are in genuine tension, which is why this is an objective worth optimizing rather than a triviality. Reconstruction would be easiest if $\bm{z}$ carried a great deal of information about $\bm{x}$ — in the extreme, if the encoder simply copied $\bm{x}$ into $\bm{z}$, reconstruction would be trivial. But then the KL divergence between $q_\phi(\bm{z} \mid \bm{x})$ and the information-free prior would be enormous, and the regularization term would be badly penalized. The objective balances the two.

At this point, if you are content to take the two terms on faith, you have everything you need to train a VAE: an architecture (two neural networks), an objective (the one above), a dataset, and an optimizer. But we can go deeper and ask what probabilistic model underlies this generative model, and show that the objective is not arbitrary — it falls out of an analysis.

### A probabilistic model and the ELBO {#sec-vae-elbo}

By a **probabilistic model** we just mean the process by which we suppose data points are generated. The autoregressive language model's process was: sample $x_1$, then $x_2$ given $x_1$, then $x_3$ given $x_2, x_1$, and so on. The VAE's generative process is:

$$\bm{z} \sim p(\bm{z}), \qquad \bm{x} \sim p(\bm{x} \mid \bm{z}).$$

Conceptually: to draw a new handwritten digit, first sample the salient factors — which digit, how thick the pen, how much slant, whether the $1$ has its flag — all encoded in $\bm{z}$, then sample an image conditioned on those factors. This is not so far from how a person decides what to draw before drawing it.

Having written down a probabilistic model, the maximum-likelihood framework asks us to maximize the probability the model assigns to the data we actually observed. We tune the parameters $\theta$ of $p_\theta$ so that it assigns high probability to the real data points on hand — exactly as the language model was trained to assign high probability to real sequences of text (Lecture 6). Being precise, we maximize $\mathbb{E}_{\bm{x} \sim p(\bm{x})}[\log p_\theta(\bm{x})]$ over $\theta$; below we use the shorthand $\log p(\bm{x})$ for a single data point.

Under the generative model, we can write $\log p(\bm{x})$ by **marginalizing** out the latent:

$$\log p(\bm{x}) = \log \int p(\bm{x} \mid \bm{z})\, p(\bm{z}) \, d\bm{z}.$$

This looks like the start of an objective, but it is intractable. To evaluate it we would have to consider every possible value of $\bm{z}$, compute $p(\bm{x} \mid \bm{z})$ for each, weight by $p(\bm{z})$, and sum — and with continuous $\bm{z}$ that is a sum over infinitely many possibilities. Much of generative modeling is about approximating this marginalization or constraining the model so that it is tractable. We use one such approximation: **variational inference**.

Variational inference does something that on its face seems trivial: to maximize a quantity, we instead maximize a *lower bound* on it. If some other quantity is always $\le \log p(\bm{x})$, then pushing that lower quantity up tends to push $\log p(\bm{x})$ up as well. An easy way to build a lower bound on any quantity is to subtract something non-negative from it, and the KL divergence is always non-negative. So we subtract a KL divergence:

$$
\log p(\bm{x}) \;\ge\; \log p(\bm{x}) - \operatorname{KL}\!\big(q_\phi(\bm{z} \mid \bm{x}) \,\big\|\, p(\bm{z} \mid \bm{x})\big).
$$

The right-hand side is the **evidence lower bound** (ELBO). The distribution $q_\phi(\bm{z} \mid \bm{x})$ it introduces is the **approximate posterior** — our encoder. Maximizing the ELBO simultaneously pushes up the data probability $\log p(\bm{x})$ and pushes the approximate posterior toward the *true* posterior $p(\bm{z} \mid \bm{x})$ [@kingma2014auto; @rezende2014stochastic].

As written, the ELBO is still not something we can optimize: it contains $\log p(\bm{x})$, which we cannot compute, and the true posterior $p(\bm{z} \mid \bm{x})$, which we also do not have. The remaining work is algebra — no further approximations. Write the troublesome KL divergence using its definition,

$$
\operatorname{KL}\!\big(q_\phi(\bm{z} \mid \bm{x}) \,\big\|\, p(\bm{z} \mid \bm{x})\big)
= \mathbb{E}_q\big[\log q_\phi(\bm{z} \mid \bm{x})\big] - \mathbb{E}_q\big[\log p(\bm{z} \mid \bm{x})\big],
$$

and rewrite the true posterior with **Bayes' rule**, $p(\bm{z} \mid \bm{x}) = \dfrac{p(\bm{x} \mid \bm{z})\, p(\bm{z})}{p(\bm{x})}$. Substituting into the second term and using $\log$ of a product/quotient as a sum/difference of logs,

$$
\mathbb{E}_q\big[\log p(\bm{z} \mid \bm{x})\big]
= \mathbb{E}_q\big[\log p(\bm{x} \mid \bm{z})\big] + \mathbb{E}_q\big[\log p(\bm{z})\big] - \underbrace{\mathbb{E}_q\big[\log p(\bm{x})\big]}_{\log p(\bm{x})}.
$$

The last term simplifies because $\log p(\bm{x})$ has no $\bm{z}$ in it, so the expectation over $\bm{z}$ leaves it unchanged. Substituting all of this back into the ELBO, the two $\log p(\bm{x})$ terms cancel — and $\log p(\bm{x})$ was exactly the term we could not compute:

$$
\log p(\bm{x}) \;\ge\;
\underbrace{\mathbb{E}_q\big[\log p_\theta(\bm{x} \mid \bm{z})\big]}_{\text{reconstruction}}
\;-\;
\underbrace{\operatorname{KL}\!\big(q_\phi(\bm{z} \mid \bm{x}) \,\big\|\, p(\bm{z})\big)}_{\text{regularization}}.
$$

Every distribution here is one we have access to: the encoder $q_\phi$, the decoder $p_\theta$ (which we model, replacing the intractable $p(\bm{x} \mid \bm{z})$), and the prior $p(\bm{z})$. This is exactly the objective we wrote down earlier — applying variational inference to the VAE's generative model recovers it.

### VAEs in practice {#sec-vae-practice}

Nothing in the derivation requires neural networks, but in practice the encoder and decoder are neural networks, and we train them by gradient-based learning. Since we are *maximizing* an objective, this is gradient **ascent** — $\bm{\theta} \leftarrow \bm{\theta} + \eta \nabla_{\bm{\theta}}(\cdot)$, a plus rather than a minus — differentiating the objective with respect to every parameter and stepping to increase it. The standard practical choices are:

1. **The encoder and decoder $q_\phi(\bm{z} \mid \bm{x})$, $p_\theta(\bm{x} \mid \bm{z})$ are neural networks.**
2. **The prior $p(\bm{z})$ is a zero-mean, identity-covariance Gaussian** — the default uninformative choice.
3. **The approximate posterior $q_\phi(\bm{z} \mid \bm{x})$ is a diagonal-covariance Gaussian.**

The third choice fixes what "output the parameters of a distribution" means concretely. If $\dim(\bm{z}) = d$ and $q_\phi$ is a diagonal Gaussian, the encoder must output $2d$ numbers: $d$ for the mean $\bm{\mu}$ and $d$ for the diagonal of the covariance. A mean can be any real number, so those $d$ units get no output nonlinearity. A variance must be positive, so the $d$ variance units pass through a nonlinearity with positive outputs — a standard choice is the **softplus**, which resembles a ReLU but is smooth.

With a Gaussian prior and a diagonal-Gaussian posterior, the KL term has a closed form. Writing the objective's regularization term with its sign, it becomes (up to a factor of $\tfrac{1}{2}$)

$$
\tfrac{1}{2}\sum_{j=1}^{d} \Big(1 + \log \sigma_j^2 - \mu_j^2 - \sigma_j^2\Big),
$$

which we are maximizing. The $-\mu_j^2$ term is maximized at $\mu_j = 0$, pulling each mean toward the zero-mean prior. The remaining $1 + \log \sigma_j^2 - \sigma_j^2$ is maximized at $\sigma_j = 1$, pulling each variance toward the identity-covariance prior. So the optimum of the KL term is $\bm{\mu} = \bm{0}$, $\bm{\sigma} = \bm{1}$ — exactly the prior, as it should be.

```{=html}
<div style="overflow-x:auto; margin:1.5rem 0;">
<svg viewBox="0 0 520 300" width="100%" style="max-width:520px;display:block;margin:auto;font-family:Helvetica,'Helvetica Neue',Arial,sans-serif;"><defs><marker id="a" markerWidth="7" markerHeight="7" refX="6" refY="3.5" orient="auto"><path d="M0,0 L0,7 L7,3.5 z" fill="#888"/></marker></defs><text x="20" y="30" font-size="22" font-weight="600" fill="#333">KL term per dimension: 1 + log(&#963;&#178;) &#8722; &#963;&#178;</text><line x1="70" y1="114.41860465116278" x2="480" y2="114.41860465116278" stroke="#ccc" stroke-width="1"/><line x1="70" y1="240" x2="70" y2="60" stroke="#888" stroke-width="1.3"/><line x1="70" y1="240" x2="480" y2="240" stroke="#888" stroke-width="1.3"/><polyline points="82.4,240.0 83.8,240.0 85.1,240.0 86.5,238.0 87.9,232.3 89.3,227.1 90.6,222.1 92.0,217.5 93.4,213.1 94.8,209.0 96.1,205.1 97.5,201.3 98.9,197.8 100.3,194.4 101.6,191.2 103.0,188.2 104.4,185.2 105.8,182.4 107.1,179.7 108.5,177.1 109.9,174.6 111.3,172.2 112.7,169.9 114.0,167.7 115.4,165.6 116.8,163.5 118.2,161.5 119.5,159.6 120.9,157.8 122.3,156.0 123.7,154.2 125.0,152.6 126.4,151.0 127.8,149.4 129.2,147.9 130.5,146.4 131.9,145.0 133.3,143.7 134.7,142.3 136.0,141.1 137.4,139.8 138.8,138.6 140.2,137.5 141.5,136.4 142.9,135.3 144.3,134.2 145.7,133.2 147.0,132.2 148.4,131.3 149.8,130.4 151.2,129.5 152.6,128.7 153.9,127.9 155.3,127.1 156.7,126.3 158.1,125.6 159.4,124.9 160.8,124.2 162.2,123.6 163.6,122.9 164.9,122.3 166.3,121.8 167.7,121.2 169.1,120.7 170.4,120.2 171.8,119.7 173.2,119.3 174.6,118.8 175.9,118.4 177.3,118.0 178.7,117.7 180.1,117.3 181.4,117.0 182.8,116.7 184.2,116.4 185.6,116.1 186.9,115.9 188.3,115.7 189.7,115.5 191.1,115.3 192.4,115.1 193.8,115.0 195.2,114.8 196.6,114.7 198.0,114.6 199.3,114.6 200.7,114.5 202.1,114.5 203.5,114.4 204.8,114.4 206.2,114.4 207.6,114.5 209.0,114.5 210.3,114.6 211.7,114.6 213.1,114.7 214.5,114.8 215.8,114.9 217.2,115.1 218.6,115.2 220.0,115.4 221.3,115.6 222.7,115.8 224.1,116.0 225.5,116.2 226.8,116.5 228.2,116.7 229.6,117.0 231.0,117.3 232.3,117.6 233.7,117.9 235.1,118.2 236.5,118.6 237.9,118.9 239.2,119.3 240.6,119.7 242.0,120.1 243.4,120.5 244.7,120.9 246.1,121.3 247.5,121.8 248.9,122.3 250.2,122.7 251.6,123.2 253.0,123.7 254.4,124.2 255.7,124.8 257.1,125.3 258.5,125.9 259.9,126.4 261.2,127.0 262.6,127.6 264.0,128.2 265.4,128.8 266.7,129.5 268.1,130.1 269.5,130.8 270.9,131.4 272.2,132.1 273.6,132.8 275.0,133.5 276.4,134.2 277.8,134.9 279.1,135.7 280.5,136.4 281.9,137.2 283.3,138.0 284.6,138.8 286.0,139.6 287.4,140.4 288.8,141.2 290.1,142.0 291.5,142.9 292.9,143.7 294.3,144.6 295.6,145.5 297.0,146.4 298.4,147.3 299.8,148.2 301.1,149.1 302.5,150.0 303.9,151.0 305.3,152.0 306.6,152.9 308.0,153.9 309.4,154.9 310.8,155.9 312.1,156.9 313.5,157.9 314.9,159.0 316.3,160.0 317.7,161.1 319.0,162.2 320.4,163.2 321.8,164.3 323.2,165.4 324.5,166.5 325.9,167.7 327.3,168.8 328.7,169.9 330.0,171.1 331.4,172.3 332.8,173.4 334.2,174.6 335.5,175.8 336.9,177.0 338.3,178.2 339.7,179.5 341.0,180.7 342.4,182.0 343.8,183.2 345.2,184.5 346.5,185.8 347.9,187.1 349.3,188.4 350.7,189.7 352.0,191.0 353.4,192.3 354.8,193.7 356.2,195.0 357.6,196.4 358.9,197.8 360.3,199.2 361.7,200.6 363.1,202.0 364.4,203.4 365.8,204.8 367.2,206.3 368.6,207.7 369.9,209.2 371.3,210.6 372.7,212.1 374.1,213.6 375.4,215.1 376.8,216.6 378.2,218.1 379.6,219.6 380.9,221.2 382.3,222.7 383.7,224.3 385.1,225.8 386.4,227.4 387.8,229.0 389.2,230.6 390.6,232.2 391.9,233.8 393.3,235.4 394.7,237.1 396.1,238.7 397.4,240.0 398.8,240.0 400.2,240.0 401.6,240.0 403.0,240.0 404.3,240.0 405.7,240.0 407.1,240.0 408.5,240.0 409.8,240.0 411.2,240.0 412.6,240.0" fill="none" stroke="#4477AA" stroke-width="2.5"/><circle cx="204.83221476510064" cy="114.41860465116278" r="4.5" fill="#EE6677"/><line x1="204.83221476510064" y1="114.41860465116278" x2="204.83221476510064" y2="240" stroke="#EE6677" stroke-width="1" stroke-dasharray="3,3"/><text x="204.83221476510064" y="102.41860465116278" text-anchor="middle" font-size="12" font-weight="600" fill="#EE6677">max at &#963;=1</text><line x1="136.04026845637583" y1="240" x2="136.04026845637583" y2="244" stroke="#888" stroke-width="1"/><text x="136.04026845637583" y="258" text-anchor="middle" font-size="11" fill="#666">0.5</text><line x1="204.83221476510064" y1="240" x2="204.83221476510064" y2="244" stroke="#888" stroke-width="1"/><text x="204.83221476510064" y="258" text-anchor="middle" font-size="11" fill="#666">1</text><line x1="273.6241610738255" y1="240" x2="273.6241610738255" y2="244" stroke="#888" stroke-width="1"/><text x="273.6241610738255" y="258" text-anchor="middle" font-size="11" fill="#666">1.5</text><line x1="342.41610738255036" y1="240" x2="342.41610738255036" y2="244" stroke="#888" stroke-width="1"/><text x="342.41610738255036" y="258" text-anchor="middle" font-size="11" fill="#666">2</text><line x1="411.20805369127515" y1="240" x2="411.20805369127515" y2="244" stroke="#888" stroke-width="1"/><text x="411.20805369127515" y="258" text-anchor="middle" font-size="11" fill="#666">2.5</text><line x1="480.0" y1="240" x2="480.0" y2="244" stroke="#888" stroke-width="1"/><text x="480.0" y="258" text-anchor="middle" font-size="11" fill="#666">3</text><text x="275.0" y="278" text-anchor="middle" font-size="13" fill="#333">&#963; (per-dimension standard deviation)</text><line x1="66" y1="72.55813953488374" x2="70" y2="72.55813953488374" stroke="#888" stroke-width="1"/><text x="61" y="76.55813953488374" text-anchor="end" font-size="11" fill="#666">1</text><line x1="66" y1="114.41860465116278" x2="70" y2="114.41860465116278" stroke="#888" stroke-width="1"/><text x="61" y="118.41860465116278" text-anchor="end" font-size="11" fill="#666">0</text><line x1="66" y1="156.27906976744185" x2="70" y2="156.27906976744185" stroke="#888" stroke-width="1"/><text x="61" y="160.27906976744185" text-anchor="end" font-size="11" fill="#666">-1</text><line x1="66" y1="198.13953488372093" x2="70" y2="198.13953488372093" stroke="#888" stroke-width="1"/><text x="61" y="202.13953488372093" text-anchor="end" font-size="11" fill="#666">-2</text><line x1="66" y1="240.0" x2="70" y2="240.0" stroke="#888" stroke-width="1"/><text x="61" y="244.0" text-anchor="end" font-size="11" fill="#666">-3</text></svg></div>
```

### The reparameterization trick {#sec-reparam}

There is one subtlety in training. To do gradient ascent we must differentiate the objective back to every parameter, and the parameters live in both the decoder and the encoder. But between them the computational graph contains a **sampling operation**: the decoder is not fed the encoder's outputs $\bm{\mu}$ and $\bm{\sigma}$ directly — it is fed a sample $\bm{z} \sim q_\phi(\bm{z} \mid \bm{x})$ drawn from the distribution those outputs parameterize. We have never before had a sampling operation inside a computational graph, and it is not obvious how to backpropagate through one.

The fix is the **reparameterization trick**, and you have likely used its core idea already. If your random number generator only produces samples from the standard normal $\mathcal{N}(\bm{0}, \bm{I})$, how do you sample from a Gaussian with a different mean and covariance? You draw standard-normal noise, scale it by the standard deviation, and shift by the mean. We do exactly that: instead of sampling $\bm{z}$ from $q_\phi(\bm{z} \mid \bm{x})$ directly, sample noise from the prior and transform it,

::: {.boxed-eq}
$$
\bm{\epsilon} \sim p(\bm{z}) = \mathcal{N}(\bm{0}, \bm{I}), \qquad z_j \leftarrow \mu_j + \sigma_j\, \epsilon_j.
$$
:::

This solves the problem by moving the stochasticity *out* of the path we need to differentiate. The sample $\bm{\epsilon}$ is now an input — noise that, as far as the gradients are concerned, is constant — and $\bm{z}$ is a deterministic, differentiable function of the encoder's outputs $\bm{\mu}$ and $\bm{\sigma}$. Backpropagating with respect to $\bm{\mu}$ and $\bm{\sigma}$ is then routine: we are just scaling and shifting noise. We do not ignore the noise — it is what makes the output equivalent to having sampled directly — but it no longer disrupts our ability to compute gradients.

The reparameterization trick works for Gaussians but not for every distribution. You cannot reparameterize sampling from a discrete distribution this way — the same obstacle that ruled out a discrete bottleneck. When you must differentiate through a discrete sample, you fall back on an approximation such as the Gumbel-softmax (concrete) relaxation [@jang2017categorical], which smoothly approximates the discrete sample, or on a score-function estimator of the REINFORCE type; these are **gradient estimators**, trading off bias and variance (Gumbel-softmax is low-variance but biased; REINFORCE is unbiased but high-variance).

::: {.callout-note}
**The likelihood model should match the data.** We took the decoder $p_\theta(\bm{x} \mid \bm{z})$ to be a Gaussian, which turned reconstruction into mean squared error — a reasonable assumption when the data values are roughly Gaussian. For binary black-and-white images it is a poor fit: a pixel's values are bimodal ($0$ or $1$), not Gaussian. There, the likelihood model should be a Bernoulli distribution, and the reconstruction term becomes a cross-entropy rather than a squared error. The choice of likelihood model is an assumption about the distribution of values in the data.

:::

## Transposed Convolution {#sec-transposed-conv}

Suppose we want a convolutional autoencoder of images. In the encoder we replace the sequence of MLPs with convolutional layers — appropriate for images, since convolutions bring translation invariance and locality (Lecture 5) — and with striding or pooling the spatial size shrinks, taking a $32 \times 32$ input down to, say, a $4 \times 4 \times 8$ activation volume that serves as the latent code. But the decoder must map back up to $32 \times 32$. Every convolution we have seen so far makes feature maps *smaller* (through striding, padding, and boundary effects); we have no convolution-like operation that makes them *bigger*. The **transposed convolution** is that operation, and it has been lurking implicitly since we introduced convolutional networks.

Start from an ordinary convolution. Let $\bm{x}$ be a $4 \times 4$ image and $\bm{k}$ a $2 \times 2$ kernel, and apply a stride-$2$ convolution to get a $2 \times 2$ output $\bm{y}$. A convolution is a linear operation, so it can be written as a matrix multiply. Flatten the input image into a vector with $\operatorname{vec}(\cdot)$ (stacking rows, say), and there is a **convolution matrix** $\bm{C}$ with

$$
\bm{C}\, \operatorname{vec}(\bm{x}) = \operatorname{vec}(\bm{y}), \qquad \bm{C} \in \mathbb{R}^{4 \times 16}.
$$

Here $\bm{C}$ maps the $16$-dimensional flattened input to the $4$-dimensional flattened output. Each row of $\bm{C}$ holds the kernel entries $k_{00}, k_{01}, k_{10}, k_{11}$ placed in the positions that one output pixel reads from, with zeros everywhere else — so $\bm{C}$ is mostly zeros. We could implement any convolution this way, as an explicit (if wasteful) matrix-vector product.

The **transposed convolution** is what happens when we transpose that matrix. Take a $2 \times 2$ input $\bm{z}$, and multiply by $\bm{C}^\top \in \mathbb{R}^{16 \times 4}$, which maps a $4$-dimensional input to a $16$-dimensional output — a $4 \times 4$ image:

::: {.boxed-eq}
$$
\bm{C}^\top \operatorname{vec}(\bm{z}) = \operatorname{vec}(\bm{y}), \qquad \bm{C}^\top \in \mathbb{R}^{16 \times 4}.
$$
:::

So where a stride-$2$ convolution took $4 \times 4$ to $2 \times 2$, the corresponding transposed convolution takes $2 \times 2$ back up to $4 \times 4$. Working out what $\bm{C}^\top$ does to $\bm{z}$ gives a useful mental picture: each entry $z_{ij}$ of the input multiplies the whole kernel, and the result is **pasted into the output** at the corresponding location. Take $z_{00}$, scale the kernel by it, paste; move to $z_{01}$, scale the kernel, paste; and so on across the input.

```{=html}
<div style="overflow-x:auto; margin:1.5rem 0;">
<svg viewBox="0 0 680 340" width="100%" style="max-width:680px;display:block;margin:auto;font-family:Helvetica,'Helvetica Neue',Arial,sans-serif;"><defs><marker id="a" markerWidth="7" markerHeight="7" refX="6" refY="3.5" orient="auto"><path d="M0,0 L0,7 L7,3.5 z" fill="#888"/></marker></defs><text x="20" y="30" font-size="22" font-weight="600" fill="#333">Transposed convolution: paste kernel, scaled by input</text><text x="40" y="108" font-size="13" fill="#666">input z (2&#215;2)</text><rect x="40" y="120" width="34" height="34" fill="#DBCDF0" stroke="#8457C7" stroke-width="1.2"/><text x="57.0" y="141.0" text-anchor="middle" font-size="11" fill="#442079">z00</text><rect x="74" y="120" width="34" height="34" fill="#fff" stroke="#8457C7" stroke-width="1.2"/><text x="91.0" y="141.0" text-anchor="middle" font-size="11" fill="#442079">z01</text><rect x="40" y="154" width="34" height="34" fill="#fff" stroke="#8457C7" stroke-width="1.2"/><text x="57.0" y="175.0" text-anchor="middle" font-size="11" fill="#442079">z10</text><rect x="74" y="154" width="34" height="34" fill="#fff" stroke="#8457C7" stroke-width="1.2"/><text x="91.0" y="175.0" text-anchor="middle" font-size="11" fill="#442079">z11</text><text x="170" y="108" font-size="13" fill="#666">kernel k (2&#215;2)</text><rect x="170" y="120" width="34" height="34" fill="#C9E4DE" stroke="#57C7AE" stroke-width="1.2"/><text x="187.0" y="141.0" text-anchor="middle" font-size="11" fill="#207965">k00</text><rect x="204" y="120" width="34" height="34" fill="#C9E4DE" stroke="#57C7AE" stroke-width="1.2"/><text x="221.0" y="141.0" text-anchor="middle" font-size="11" fill="#207965">k01</text><rect x="170" y="154" width="34" height="34" fill="#C9E4DE" stroke="#57C7AE" stroke-width="1.2"/><text x="187.0" y="175.0" text-anchor="middle" font-size="11" fill="#207965">k10</text><rect x="204" y="154" width="34" height="34" fill="#C9E4DE" stroke="#57C7AE" stroke-width="1.2"/><text x="221.0" y="175.0" text-anchor="middle" font-size="11" fill="#207965">k11</text><text x="326" y="120" text-anchor="middle" font-size="15" font-weight="600" fill="#796020">z00 &#215; kernel</text><line x1="280" y1="140" x2="372" y2="140" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><text x="380" y="78" font-size="13" fill="#666">output y (4&#215;4), stride 2</text><rect x="380" y="90" width="30" height="30" fill="#C9E4DE" stroke="#C78557" stroke-width="1.1"/><text x="395.0" y="109.0" text-anchor="middle" font-size="10" fill="#207965">k00</text><rect x="410" y="90" width="30" height="30" fill="#C9E4DE" stroke="#C78557" stroke-width="1.1"/><text x="425.0" y="109.0" text-anchor="middle" font-size="10" fill="#207965">k01</text><rect x="440" y="90" width="30" height="30" fill="#fff" stroke="#C78557" stroke-width="1.1"/><rect x="470" y="90" width="30" height="30" fill="#fff" stroke="#C78557" stroke-width="1.1"/><rect x="380" y="120" width="30" height="30" fill="#C9E4DE" stroke="#C78557" stroke-width="1.1"/><text x="395.0" y="139.0" text-anchor="middle" font-size="10" fill="#207965">k10</text><rect x="410" y="120" width="30" height="30" fill="#C9E4DE" stroke="#C78557" stroke-width="1.1"/><text x="425.0" y="139.0" text-anchor="middle" font-size="10" fill="#207965">k11</text><rect x="440" y="120" width="30" height="30" fill="#fff" stroke="#C78557" stroke-width="1.1"/><rect x="470" y="120" width="30" height="30" fill="#fff" stroke="#C78557" stroke-width="1.1"/><rect x="380" y="150" width="30" height="30" fill="#fff" stroke="#C78557" stroke-width="1.1"/><rect x="410" y="150" width="30" height="30" fill="#fff" stroke="#C78557" stroke-width="1.1"/><rect x="440" y="150" width="30" height="30" fill="#fff" stroke="#C78557" stroke-width="1.1"/><rect x="470" y="150" width="30" height="30" fill="#fff" stroke="#C78557" stroke-width="1.1"/><rect x="380" y="180" width="30" height="30" fill="#fff" stroke="#C78557" stroke-width="1.1"/><rect x="410" y="180" width="30" height="30" fill="#fff" stroke="#C78557" stroke-width="1.1"/><rect x="440" y="180" width="30" height="30" fill="#fff" stroke="#C78557" stroke-width="1.1"/><rect x="470" y="180" width="30" height="30" fill="#fff" stroke="#C78557" stroke-width="1.1"/><text x="440" y="215.0" text-anchor="middle" font-size="11" fill="#794520"></text><text x="20" y="300" font-size="13" fill="#666">Each input entry z&#7522;&#11388; scales a copy of the kernel, pasted at the matching output location; copies</text><text x="20" y="318" font-size="13" fill="#666">sum where they overlap. This is the transpose C&#8868; of the convolution matrix C, and it grows the map.</text></svg></div>
```

::: {.definition-block}
**Definition.** A *transposed convolution* applies the transpose $\bm{C}^\top$ of a convolution's matrix $\bm{C}$. Equivalently, it pastes a copy of the kernel into the output for each input entry, scaled by that entry, summing where copies overlap. With a stride, it *increases* the spatial size — the inverse of the size change a strided convolution produces.

:::

This mental picture extends to channels: a multi-channel transposed convolution pastes each channel's kernel scaled by that channel's input and sums the results. If one kernel looks like a cat's face and another like a cat's body, then an input that is one at the face location and one at the body location, and zero elsewhere, pastes the face and the body into place and sums them into a cat.

Where has the transposed convolution been hiding? In the **backward pass**. Recall that a dense layer's forward pass multiplies by $\bm{W}$ and its backward pass multiplies by $\bm{W}^\top$. Since a convolution's forward pass is a multiply by $\bm{C}$, its backward pass is a multiply by $\bm{C}^\top$ — a transposed convolution. It is the operation that computes gradients through a convolution, and it has been there implicitly all along.

## The U-Net {#sec-unet}

For images we have mostly built networks that take an image in and produce a classification. It is also useful to take an image in and produce an *image* out — for example, **semantic segmentation**, where we classify every pixel (these pixels are people, those are chairs), producing an output the same shape as the input.

Here the tension between the two size changes is exactly the point. A convolutional network that shrinks the representation is good for aggregating *global* information and growing the receptive field — we would like to know the scene is a classroom before deciding which pixels are people and which are chairs. But to make a dense, per-pixel prediction we then need to grow the representation back up to full resolution, which is what the transposed convolution provides. The standard architecture for aggregating global information and then upsampling to the input's size is the **U-Net** [@ronneberger2015unet].

```{=html}
<div style="overflow-x:auto; margin:1.5rem 0;">
<svg viewBox="0 0 760 340" width="100%" style="max-width:760px;display:block;margin:auto;font-family:Helvetica,'Helvetica Neue',Arial,sans-serif;"><defs><marker id="a" markerWidth="7" markerHeight="7" refX="6" refY="3.5" orient="auto"><path d="M0,0 L0,7 L7,3.5 z" fill="#888"/></marker></defs><text x="20" y="30" font-size="22" font-weight="600" fill="#333">U-Net</text><rect x="40" y="70" width="62" height="30" rx="6" fill="#C6DEF1" stroke="#5795C7" stroke-width="1.5"/><text x="71.0" y="89.66666666666667" text-anchor="middle" font-size="14" font-weight="600" fill="#205279">image</text><rect x="130" y="70" width="62" height="30" rx="6" fill="#C9E4DE" stroke="#57C7AE" stroke-width="1.5"/><text x="161.0" y="89.66666666666667" text-anchor="middle" font-size="14" font-weight="600" fill="#207965">conv</text><line x1="102" y1="85.0" x2="130" y2="85.0" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><rect x="140" y="110" width="62" height="30" rx="6" fill="#ECECEC" stroke="#AAAAAA" stroke-width="1.5"/><text x="171.0" y="129.66666666666666" text-anchor="middle" font-size="14" font-weight="600" fill="#666666">pool</text><line x1="161.0" y1="100" x2="171.0" y2="110" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><rect x="230" y="150" width="62" height="30" rx="6" fill="#C9E4DE" stroke="#57C7AE" stroke-width="1.5"/><text x="261.0" y="169.66666666666666" text-anchor="middle" font-size="14" font-weight="600" fill="#207965">conv</text><line x1="171.0" y1="140" x2="230" y2="165.0" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><rect x="240" y="190" width="62" height="30" rx="6" fill="#ECECEC" stroke="#AAAAAA" stroke-width="1.5"/><text x="271.0" y="209.66666666666666" text-anchor="middle" font-size="14" font-weight="600" fill="#666666">pool</text><line x1="261.0" y1="180" x2="271.0" y2="190" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><rect x="330" y="230" width="62" height="30" rx="6" fill="#C9E4DE" stroke="#57C7AE" stroke-width="1.5"/><text x="361.0" y="249.66666666666666" text-anchor="middle" font-size="14" font-weight="600" fill="#207965">conv</text><line x1="271.0" y1="220" x2="330" y2="245.0" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><rect x="340" y="270" width="62" height="30" rx="6" fill="#ECECEC" stroke="#AAAAAA" stroke-width="1.5"/><text x="371.0" y="289.6666666666667" text-anchor="middle" font-size="14" font-weight="600" fill="#666666">pool</text><line x1="361.0" y1="260" x2="371.0" y2="270" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><rect x="430" y="270" width="62" height="30" rx="6" fill="#F7D9C4" stroke="#C78557" stroke-width="1.5"/><text x="461.0" y="289.6666666666667" text-anchor="middle" font-size="14" font-weight="600" fill="#794520">conv&#8868;</text><line x1="402" y1="285.0" x2="430" y2="285.0" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><rect x="430" y="230" width="62" height="30" rx="6" fill="#FAEDCB" stroke="#C7A857" stroke-width="1.5"/><text x="461.0" y="249.66666666666666" text-anchor="middle" font-size="14" font-weight="600" fill="#796020">concat</text><line x1="461.0" y1="270" x2="461.0" y2="260" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><path d="M392,245.0 L430,245.0" fill="none" stroke="#5795C7" stroke-width="1.6"/><rect x="530" y="150" width="62" height="30" rx="6" fill="#F7D9C4" stroke="#C78557" stroke-width="1.5"/><text x="561.0" y="169.66666666666666" text-anchor="middle" font-size="14" font-weight="600" fill="#794520">conv&#8868;</text><line x1="461.0" y1="230" x2="561.0" y2="180" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><rect x="620" y="150" width="62" height="30" rx="6" fill="#FAEDCB" stroke="#C7A857" stroke-width="1.5"/><text x="651.0" y="169.66666666666666" text-anchor="middle" font-size="14" font-weight="600" fill="#796020">concat</text><line x1="592" y1="165.0" x2="620" y2="165.0" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><path d="M292,165.0 L292,134 L651.0,134 L651.0,150" fill="none" stroke="#5795C7" stroke-width="1.6"/><rect x="620" y="110" width="62" height="30" rx="6" fill="#F7D9C4" stroke="#C78557" stroke-width="1.5"/><text x="651.0" y="129.66666666666666" text-anchor="middle" font-size="14" font-weight="600" fill="#794520">conv&#8868;</text><line x1="651.0" y1="150" x2="651.0" y2="140" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><rect x="620" y="70" width="62" height="30" rx="6" fill="#FAEDCB" stroke="#C7A857" stroke-width="1.5"/><text x="651.0" y="89.66666666666667" text-anchor="middle" font-size="14" font-weight="600" fill="#796020">concat</text><line x1="651.0" y1="110" x2="651.0" y2="100" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><path d="M192,85.0 L192,54 L651.0,54 L651.0,70" fill="none" stroke="#5795C7" stroke-width="1.6"/><line x1="682" y1="85.0" x2="700" y2="85.0" stroke="#888" stroke-width="1.5" marker-end="url(#a)"/><rect x="700" y="70" width="52" height="30" rx="4" fill="#F2C6DE" stroke="#C75794" stroke-width="1.5"/><text x="726" y="89.0" text-anchor="middle" font-size="11" font-weight="600" fill="#792051">output</text><line x1="40" y1="318" x2="70" y2="318" stroke="#5795C7" stroke-width="1.6"/><text x="76" y="322" font-size="13" fill="#666">skip connection: concatenate feature maps</text><text x="400" y="322" font-size="13" fill="#666">down: conv + pool  |  up: conv&#8868; (transposed) + concat</text></svg></div>
```

The U-Net has a down path of convolutions and pooling that produces smaller and smaller feature maps, and an up path of transposed convolutions that grows them back. The essential ingredient is **concatenation**: the feature volume produced by a convolutional layer on the down path is concatenated, along the channel axis, with the same-shaped feature volume produced by the transposed convolution on the up path. As long as the shapes match going down and coming back up, we can concatenate at each level.

::: {.definition-block}
**Definition.** A *U-Net* is an encoder-decoder for images: a down path of convolution and pooling that shrinks feature maps while widening the receptive field, an up path of transposed convolutions that restores the spatial size, and **skip connections** that concatenate each down-path feature volume with the matching up-path feature volume. It maps an input image to an output image of the same size.

:::

The reason for the concatenation is that the down path discards local, high-resolution information as it aggregates global context. The skip connections carry that local information from the down path across to the up path, so the final layers can combine global context with local detail to make sharp per-pixel predictions. The architecture is named for its U shape when drawn. Beyond dense prediction like segmentation, the U-Net is also a workhorse of diffusion models, another family of generative models.

## Summary {#sec-summary}

::: {.callout-note}
**Key takeaways from Lecture 11:**

- **Unsupervised representation learning** can be framed as **compression plus reconstruction**: encode the input with as little information as possible while still being able to reconstruct it. The salient factors for reconstruction are often the most useful for downstream tasks.
- An **autoencoder** pairs an encoder $\bm{x} \to \bm{z}$ with a decoder $\bm{z} \to \tilde{\bm{x}}$, trained by a reconstruction loss such as $\lVert \bm{x} - \tilde{\bm{x}}\rVert^2$. A smaller latent does *not* guarantee compression, because a real-valued vector can hold unbounded information; a discrete latent would fix that but blocks gradients.
- A **variational autoencoder** makes the latent stochastic and adds a KL regularizer, maximizing $\mathbb{E}_{q_\phi(\bm{z} \mid \bm{x})}[\log p_\theta(\bm{x} \mid \bm{z})] - \operatorname{KL}(q_\phi(\bm{z} \mid \bm{x}) \,\|\, p(\bm{z}))$. A Gaussian likelihood makes the reconstruction term a squared error (Bernoulli for binary pixels makes it a cross-entropy); the KL term measures the information the latent carries about the input.
- The same objective is the **evidence lower bound**, obtained by applying **variational inference** to the VAE's generative model $\bm{z} \sim p(\bm{z})$, $\bm{x} \sim p(\bm{x} \mid \bm{z})$ and cancelling the intractable $\log p(\bm{x})$ and true posterior $p(\bm{z} \mid \bm{x})$ via Bayes' rule.
- In practice the prior is $\mathcal{N}(\bm{0}, \bm{I})$, the encoder is a diagonal Gaussian outputting $2d$ numbers (means, and variances through a softplus), and the KL term has a closed form optimized at $\bm{\mu} = \bm{0}$, $\bm{\sigma} = \bm{1}$. The **reparameterization trick** $z_j = \mu_j + \sigma_j \epsilon_j$, $\bm{\epsilon} \sim \mathcal{N}(\bm{0}, \bm{I})$, moves the sampling out of the differentiated path; it does not work for discrete latents.
- A **transposed convolution** applies $\bm{C}^\top$, the transpose of a convolution's matrix — equivalently, it pastes the kernel into the output scaled by each input entry, and it *grows* feature maps. It is the backward pass of a convolution.
- A **U-Net** combines a shrinking convolutional down path, a growing transposed-convolution up path, and **skip connections** that concatenate matching feature maps, producing an output image the same size as the input. It is used for dense prediction such as semantic segmentation, and in diffusion models.
:::

**Next lecture:** Deep learning engineering; fairness, accountability, and transparency.
